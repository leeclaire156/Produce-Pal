// UN & PW Info can be used by other models

/* const { Schema, model } = require('mongoose');

const userSchema = new Schema({
    username: {
        type: String, 
        required: true, 
        trim: true, 
    password: {
        type: String, 
        required: true, 
        trim: true, 
    },
});

const UserInfo = mongoose.model('UserInfo', userInfoSchema);

module.exports = UserInfo;
*/