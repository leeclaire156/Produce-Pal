/*
const { Schema, model } = require('mongoose');

const cartSchema = new Schema({
    // acquired from logged in user info - how??
    consumerId: {
        type: String,
        required: true,
    },
    cartContents: {
        type: [Object],
    },
    date: {
        type: Date, 
        default: Date.now,
    },
});

const Cart = mongoose.model('Cart', cartSchema)

module.exports = Cart;
*/

// ADD TO README CREDIT: https://copyprogramming.com/howto/insert-online-shopping-cart-items-into-mongodb-using-mongoose
   // Used for tutorial on shopping cart
